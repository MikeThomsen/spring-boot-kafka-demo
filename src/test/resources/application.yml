demo:
  topic:
    string_test: strings
    avro_test: avro-messages

kafka:
  bootstrap:
    address: localhost:9092
  consumer: avroConsumerGroup

logging:
  level:
    root: ERROR
    com.example.demokafkatest: DEBUG